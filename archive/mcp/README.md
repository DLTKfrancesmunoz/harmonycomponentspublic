# Archived MCP assets

MCP-related data, docs, scripts, and CI have been moved here. The repo no longer builds or maintains MCP data.

**Contents:** `mcp-data/`, MCP docs, scripts that generated/validated MCP specs and layouts, and the previous regenerate-data workflow.

**Note:** Archived scripts depend on `../scripts/lib/` and `../astro-parser.js` from the repo root. Running them from this directory may require path or dependency adjustments.
