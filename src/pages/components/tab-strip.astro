---
import DocsLayout from '../../layouts/DocsLayout.astro';
import ExampleSection from '../../components/ExampleSection.astro';
import Icon from '../../components/ui/Icon.astro';
---

<DocsLayout title="Tab Strip" description="Tab components for organizing content.">
  <article class="space-y-12">
    <header class="page-header page-header--compact">
      <div class="flex items-center gap-3 mb-4">
        <h1 class="page-header__title page-header__title--sm">Tab Strip</h1>
        <span class="badge badge--success">stable</span>
      </div>
      <p class="page-header__description">
        Tabs organize content into separate views where only one view is visible at a time.
      </p>
    </header>

    <nav class="article-nav">
      <a href="#examples" class="article-nav__link"><Icon name="eye" size="sm" /> Examples</a>
    </nav>

    <section id="examples" class="space-y-8">
      <h2 class="section__title">Examples</h2>

      <ExampleSection title="Basic Tabs" description="Standard tab navigation.">
        <div>
          <div class="tabs">
            <button class="tab is-active">Overview</button>
            <button class="tab">Features</button>
            <button class="tab">Pricing</button>
            <button class="tab">Reviews</button>
          </div>
          <div class="tab-panel is-active">
            <p class="text-secondary">
              This is the overview content. Click other tabs to switch views.
            </p>
          </div>
        </div>
      </ExampleSection>

      <ExampleSection title="With Icons" description="Tabs with icons.">
        <div>
          <div class="tabs">
            <button class="tab is-active">
              <Icon name="home" size="sm" class="mr-1" /> Home
            </button>
            <button class="tab">
              <Icon name="user" size="sm" class="mr-1" /> Profile
            </button>
            <button class="tab">
              <Icon name="cog" size="sm" class="mr-1" /> Settings
            </button>
          </div>
          <div class="tab-panel is-active">
            <p class="text-secondary">Home tab content.</p>
          </div>
        </div>
      </ExampleSection>

      <ExampleSection title="Disabled Tab" description="Tab that cannot be selected.">
        <div>
          <div class="tabs">
            <button class="tab is-active">Active</button>
            <button class="tab">Normal</button>
            <button class="tab tab--disabled" disabled>Disabled</button>
          </div>
        </div>
      </ExampleSection>
    </section>
  </article>
</DocsLayout>
