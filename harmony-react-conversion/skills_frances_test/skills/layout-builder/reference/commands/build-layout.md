# Build layout playbook (moved)

The full workflow for composing a page inside a converted Harmony shell is maintained as an **Agent Skill** with explicit invocation.

**Canonical source:** [`../../build-layout/SKILL.md`](../../build-layout/SKILL.md) (from repo root: `.cursor/skills/build-layout/SKILL.md`).

Update that skill when the playbook changes; do not restore a duplicate copy here.
