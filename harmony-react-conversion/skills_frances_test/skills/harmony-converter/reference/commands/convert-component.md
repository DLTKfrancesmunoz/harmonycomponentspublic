# Convert component playbook (moved)

The full workflow for converting a single Harmony component is maintained as an **Agent Skill** with explicit invocation (`disable-model-invocation: true`).

**Canonical source:** [`../../convert-component/SKILL.md`](../../convert-component/SKILL.md) (from repo root: `.cursor/skills/convert-component/SKILL.md`).

Update that skill when the playbook changes; do not restore a duplicate copy here.
